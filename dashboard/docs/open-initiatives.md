# Open initiatives (tracked by evidence map)

## coding-worker-transport

Status: **not yet built** — intentionally deferred.

autonomous code-worker transport remains intentionally deferred.
Keep durable queue/verification contracts live and add a callable local coding-worker transport before enabling autonomous repository mutation.

See local `.planning/issue-resolution-pipeline/task_plan.md` when present on a developer machine.
