# Plan — Activity Feed

## Current objective

- Keep project aligned with Central Ops registration + OpenWiki obligation.
- Maintain OpenWiki + registry alignment; run durable work through Central Ops.

## Next actions

1. Confirm OpenWiki workflow succeeds on GitHub (`openwiki-update.yml`).
2. Use Central Ops WorkItems for durable changes (when binding Pi/CO).
3. Keep this file short; deep session notes go in `.co/planning/` or `.planning/`.

## Out of scope

- Treating repo-index or Buzz portfolio boards as project identity.
- Dual status docs that contradict `registry/projects/activity-feed.yaml`.
