# Activity Feed — Central Ops project kit

See `~/central-ops/docs/PROJECT-OPERATING-KIT.md`.

This directory is the local planning/status snapshot for a **registered** Central Ops project.
Canonical identity/status still live in `~/central-ops/registry/projects/activity-feed.yaml`.
Code docs live in OpenWiki (`openwiki/`).
